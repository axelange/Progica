{% extends 'base.html.twig' %}

{% block title %}Gite{% endblock %}

{% block body %}
    <h1>Gite</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ gite.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ gite.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ gite.description }}</td>
            </tr>
            <tr>
                <th>Surface</th>
                <td>{{ gite.surface }}</td>
            </tr>
            <tr>
                <th>Bedrooms</th>
                <td>{{ gite.bedrooms }}</td>
            </tr>
            <tr>
                <th>Beds</th>
                <td>{{ gite.beds }}</td>
            </tr>
            <tr>
                <th>Price</th>
                <td>{{ gite.price }}</td>
            </tr>
            <tr>
                <th>Adress</th>
                <td>{{ gite.adress }}</td>
            </tr>
            <tr>
                <th>Zipcode</th>
                <td>{{ gite.zipcode }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_gite_index') }}">back to list</a>

    <a href="{{ path('app_gite_edit', {'id': gite.id}) }}">edit</a>

    {{ include('gite/_delete_form.html.twig') }}
{% endblock %}
